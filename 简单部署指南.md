# 🎯 三步部署到 GitHub

---

## 📋 准备工作

确保你有：
- ✅ GitHub 账号
- ✅ Git 已安装（如果没有，下载：https://git-scm.com/download/win）

---

## 🚀 超简单三步部署

### 第一步：配置你的信息

在项目目录打开 PowerShell，运行：

```powershell
cd C:\Users\Administrator\Desktop\WatusiPatch
.\setup_github.ps1
```

**会问你以下信息：**
1. GitHub 用户名（例如：john-doe）
2. 你的名字（例如：John Doe）
3. 你的邮箱（例如：john@example.com）
4. 包名后缀（直接回车使用默认：watusipatch）

✅ 完成后会自动更新所有配置文件

---

### 第二步：创建 GitHub 仓库

1. 打开浏览器，访问：https://github.com/new

2. 填写信息：
   - **Repository name**: `WatusiPatch`
   - **Description**: `Complete WhatsApp anti-detection and network optimization patch`
   - **可见性**: Public（推荐）
   - ❌ **不要勾选任何选项**（不要勾选 README、.gitignore、License）

3. 点击绿色按钮 **"Create repository"**

---

### 第三步：一键部署

返回 PowerShell，运行：

```powershell
.\deploy_to_github.ps1
```

脚本会自动完成：
- ✅ 初始化 Git
- ✅ 添加文件
- ✅ 创建提交
- ✅ 推送到 GitHub
- ✅ 创建 Release tag

**如果提示输入密码：**
- 用户名：你的 GitHub 用户名
- 密码：你的 GitHub 密码或 Personal Access Token

---

## 🎉 完成！

部署完成后，访问：

- **项目主页**: `https://github.com/你的用户名/WatusiPatch`
- **查看编译**: `https://github.com/你的用户名/WatusiPatch/actions`
- **下载 .deb**: `https://github.com/你的用户名/WatusiPatch/releases`（编译完成后约5-10分钟）

---

## ❓ 常见问题

### Q1: 推送时提示需要 Personal Access Token？

**解决方法：**

1. 访问：https://github.com/settings/tokens
2. 点击 **"Generate new token (classic)"**
3. 填写：
   - Note: `WatusiPatch`
   - Expiration: 选择期限
   - ✅ 勾选 `repo`（所有选项）
4. 点击底部 **"Generate token"**
5. **复制生成的 token**（只显示一次！）
6. 推送时使用 token 作为密码

### Q2: Git 推送失败？

**检查：**
- GitHub 仓库是否已创建
- 仓库名是否为 `WatusiPatch`
- 网络连接是否正常

**重试：**
```powershell
git push -u origin main --force
```

### Q3: Actions 编译失败？

1. 访问 Actions 页面查看错误日志
2. 通常是 Theos 或 SDK 下载问题
3. 可以手动重新运行 workflow

### Q4: 想修改代码后重新推送？

```powershell
git add .
git commit -m "update: 修改说明"
git push
```

发布新版本：
```powershell
git tag -a v2.0.1 -m "v2.0.1"
git push origin v2.0.1
```

---

## 📞 需要帮助？

- 查看完整文档：`DEPLOYMENT_GUIDE.md`
- 检查清单：`FINAL_CHECKLIST.md`
- GitHub Issues：创建仓库后在 Issues 页面提问

---

## ✨ 快速命令参考

```powershell
# 配置信息
.\setup_github.ps1

# 一键部署
.\deploy_to_github.ps1

# 查看状态
git status

# 推送更新
git add .
git commit -m "update"
git push

# 发布新版本
git tag -a v2.1.0 -m "v2.1.0"
git push origin v2.1.0
```

---

**就这么简单！三步完成部署！🚀**
